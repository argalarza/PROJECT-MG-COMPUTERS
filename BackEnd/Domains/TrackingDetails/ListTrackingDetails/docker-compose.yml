# auto-generated by ginshot
# Uncomment if needed
# version: '3.8'

services:
  trackingdetails-db:
    build:
      context: ./config/trackingdetails
      dockerfile: Dockerfile
    container_name: trackingdetails-db
    environment:
      MONGO_INITDB_ROOT_USERNAME: user
      MONGO_INITDB_ROOT_PASSWORD: password
    ports:
      - "27017:27017"
    networks:
      - app-network

  # list-tracking-details API Container
  list-tracking-details:
    build: .
    ports:
      - "80:80"
    container_name: list-tracking-details
    environment:
      - GIN_MODE=release
      - trackingdetails_URI=mongodb://user:password@trackingdetails-db:27017
    volumes:
      - .:/app
    networks:
      - app-network
    restart: unless-stopped
    depends_on:
      - trackingdetails-db

networks:
  app-network:
    driver: bridge
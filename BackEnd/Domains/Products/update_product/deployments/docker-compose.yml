# auto-generated by ginshot
# Uncomment if needed
# version: '3.8'

services:
  products-db:
    build:
      context: ../../database/products
      dockerfile: Dockerfile
    container_name: products-db
    environment:
      MONGO_INITDB_ROOT_USERNAME: user
      MONGO_INITDB_ROOT_PASSWORD: password
    ports:
      - "27017:27017"
    networks:
      - app-network

  # update_product API Container
  update_product:
    build: ..
    ports:
      - "80:80"
    container_name: update_product
    environment:
      - GIN_MODE=release
      - products_URI=mongodb://user:password@products-db:27017
    volumes:
      - .:/app
    networks:
      - app-network
    restart: unless-stopped
    depends_on:
      - products-db

networks:
  app-network:
    driver: bridge